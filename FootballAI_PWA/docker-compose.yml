services:
  footballai:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - ./server:/app
    command: sh -c "npm install && node server.js"
    ports:
      - "8787:8787"
    env_file:
      - ./server/.env
